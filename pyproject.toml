[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flash-vla"
version = "0.1.0"
description = "Hardware-specialized VLA inference targets built with TileLang and CUDA Graphs"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["tilelang", "cuda", "hopper", "gpu-kernels", "vla", "pi0", "pi05", "inference"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Environment :: GPU :: NVIDIA CUDA",
]

# TileLang is pinned: the kernels depend on documented lowering behaviour of this
# release (fragment layouts, the pipelined-body inlining rule, the combine-tile
# limit in FlashDecoding). Torch is deliberately unpinned so the CUDA build can be
# chosen to match the driver; developed against 2.11.0+cu130.
dependencies = [
    "tilelang==0.1.11",
    "torch>=2.4",
]

[project.optional-dependencies]
bench = []
# Pi0.5 puts the state in the prompt, so its prompt path needs a tokenizer.
pi05 = ["sentencepiece", "numpy"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

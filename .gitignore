__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.cache/

# benchmark and profiler output
*.pt
*.json
# candidate plans are tracked (lab/plans/README.md)
!lab/plans/*.json
*.ncu-rep
*.nsys-rep
artifacts/
logs/

# profiler capture artifacts (reports are regenerated from the raw traces)
profiles/
*.sqlite

# scratch prompt notes
instruction.md

# external checkout, not a submodule of this project
third_party/sglang/

# kernel-wiki carries SHA-pinned upstream bundles; they are content, not
# build output.
!.claude/skills/kernel-wiki/artifacts/

# Validated publication views; raw reports remain under artifacts/.
!results/**/*.json

# Machine-local job launchers live outside version control.
/sbatch/
/lab/sbatch/
